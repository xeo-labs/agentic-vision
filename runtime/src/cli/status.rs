//! Show status of the running Cortex daemon.

use anyhow::Result;

/// Connect to socket and display runtime status.
pub async fn run() -> Result<()> {
    println!("cortex status: not yet implemented");
    Ok(())
}
