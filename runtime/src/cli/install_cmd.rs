//! `cortex install` — download Chrome for Testing.

use anyhow::Result;

/// Download and install Chrome for Testing into ~/.cortex/chromium/.
pub async fn run() -> Result<()> {
    println!("cortex install: not yet implemented");
    println!("  Will download Chrome for Testing to ~/.cortex/chromium/");
    Ok(())
}
