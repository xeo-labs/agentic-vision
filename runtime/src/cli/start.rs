//! Start the Cortex daemon process.

use anyhow::Result;

/// Start the Cortex daemon: bind socket, launch browser pool, serve requests.
pub async fn run() -> Result<()> {
    println!("cortex start: not yet implemented");
    Ok(())
}
